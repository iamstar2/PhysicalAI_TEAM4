# -*- coding: utf-8 -*-
"""목적지 사전 — `01_작업장_선정_및_목적지_정의서.md` §5.3 그대로 옮긴 것.

주 키워드(정확 매칭)와 보조 키워드(부분 매칭)를 분리한다. 점수 차이는 `matcher.py` 참조.

**여기에는 키워드와 장소 이름만 둔다.** 방문자에게 들려주는 문구는 전부 `phrases.json` 에
있다 — 멘트를 다듬을 때 파일 두 개를 오가지 않도록 분리했다.
`name` 은 LLM 분류기 프롬프트에서 "이 key 가 어디인지" 를 설명하는 데 쓴다.
"""

DESTINATIONS = {
    # "reception"(안내데스크)은 **B 의 목적지가 아니다.** B 가 바로 거기 서 있기 때문에,
    # 방문자가 B 에게 "안내데스크 어디예요" 라고 물을 일이 없다.
    # 공용 스키마의 `Destination` enum 에는 그대로 남는다 — **C 가 에스코트를 마치고
    # 복귀할 때 쓰는 값**이다. B 가 그 값을 발행하지 않을 뿐이다.
    # 그래도 누가 물으면 `phrases.json` 의 음성안내가 "여기가 안내데스크예요" 로 답한다.
    "meeting_room_1": {
        "primary": ["회의실"],
        "secondary": ["미팅", "회의", "미팅룸"],
        "name": "회의실 1",
    },
    "safety_training_room": {
        "primary": ["안전교육장", "교육장"],
        "secondary": ["교육", "안전교육", "신규교육"],
        "name": "안전교육장",
    },
    "inbound_dock": {
        "primary": ["입고", "입고장", "입고도크"],
        # "도크"는 원문 사전(§5.3)에 없었는데, 같은 문서 모호성 표의 예시
        # ("도크 갈게요" → 선택형 재질문)가 성립하려면 입고·출고 양쪽에 있어야 한다.
        # 실측에서 이 갭 때문에 "도크 갈게요"가 매칭 0개로 빠졌다 (LOG-29).
        # "들어왔" 제거 (2026-09-18, LOG-40). 자모 편집거리 2 안에서 "들었"·"들어요"를
        # 잡아 "힘들었어요"·"만들었어요" 같은 일상 표현까지 입고 도크로 확정시켰다.
        # "짐 들어왔어요" 류는 LLM 폴백이 잡는다 — 룰이 틀리게 성공하는 것보다 낫다.
        "secondary": ["하차", "납품", "배송", "도크"],
        "name": "입고 도크",
    },
    "outbound_dock": {
        "primary": ["출고", "출고장", "상차"],
        # "실으러" 제거 — "실으러 온 건 아니고요"(부정문)까지 출고로 확정시켰다 (LOG-40)
        "secondary": ["픽업", "출차", "상차하러", "도크"],
        "name": "출고 도크",
    },
    "inspection_area": {
        # "검사" 추가 (LOG-40): 실사람 발화 "검수하러 왔습니다"가 STT에서 "검사~"로
        # 인식되는 것을 실측 확인. 방문자가 실제로 쓰는 표현이기도 하다.
        "primary": ["검수장", "검수", "검사"],
        # "확인하러" 제거 — 아무 용건에나 쓰는 말이라 "확인하러 왔는데요"를 검수장으로 확정시켰다
        "secondary": ["검품", "품질"],
        "name": "검수장",
    },
    "elevator_hall": {
        # **2층 목적지는 전부 여기로 보낸다** (`01` §5.1 설계 원칙 — MechDog 은 계단·엘리베이터를
        # 못 쓰므로 에스코트는 1층까지만). 2층에 뭐가 있는지 확정된 게 없어서
        # "사무실"·"사장실" 정도로 단순하게 잡는다 — 방문자가 실제로 쓸 표현이면서
        # 1층 목적지 이름과 안 겹친다.
        # `사장실` 을 뺐다 (2026-09-18) — **`화장실` 의 `장실` 과 자모 거리 1**이라
        # "화장실 어디예요" 를 2층(엘리베이터)으로 보냈다. 화장실 가려는 사람을
        # 엘리베이터로 안내하는 셈이라 위험하다. 2층에는 사무실만 있는 것으로 정리했다.
        "primary": ["2층", "이층", "위층", "엘리베이터", "사무실"],
        "secondary": ["관리팀", "사무동", "올라가", "위에"],
        "name": "엘리베이터 홀 (2층 목적지는 전부 여기로)",
    },
    "exit_gate": {
        # **"출구" 를 빼지 않는다.** 우리 멘트에서는 안 쓰지만(phrases.json 참고 — 방문자가
        # 따라 말하다 "출고" 로 뒤집히는 걸 막으려고), **방문자는 실제로 "출구" 라고 말한다.**
        # 여기서 빼면 "출구 어디예요" 를 아예 못 알아듣는다. 오인식 방어는 `_apply_dock_rule`
        # (도크 동반 시 억제)과 확인 질의가 맡는다.
        "primary": ["출구", "나가"],
        # "돌아갈"·"끝났" 제거 — "이제 끝났어요"·"돌아갈게요" 같은 대화 마무리 표현까지
        # 출구로 확정시켰다 (LOG-40).
        # "퇴장" 제거 (2026-09-18) — 2글자라 편집거리 1 안에서 "센터장"의 "터장"을 잡아
        # "센터장님 뵈러 왔어요"를 출구로 오확정시켰다. `LOG-40`·`LOG-48`과 같은 패턴.
        "secondary": ["나가는 곳", "나가는 문", "나가는 길"],
        "name": "출구 (나가는 곳)",
    },
}

# STT 도메인 바이어싱 문자열.
#
# 주 키워드만 넣었을 때 "상차 왔어요"·"납품 왔어요" 같은 보조 키워드 발화를 놓치는 것을
# 실측으로 확인해서(`09_작업기록 LOG-27`), **보조 키워드까지 전부 포함**한 버전이다.
#
# **"출구" 는 일부러 뺐다.** 프롬프트에 넣으면 whisper 가 그 단어를 쓰도록 편향되는데,
# `출구`↔`출고` 는 STT 가 자주 뒤집는 쌍이라 **방문자가 "출고" 라고 말해도 "출구" 로 쓸
# 확률이 올라간다.** 그러면 출고 도크로 가려는 사람을 정반대인 정문으로 보내게 된다
# (`LOG-27`). 대신 `나가는 곳`·`나가는 문` 을 넣어 같은 의도를 다른 발음으로 유도한다.
# 매칭 키워드에는 `출구` 를 남겨 뒀다 — 그래도 "출구" 로 인식되면 잡아야 하기 때문이다.
INITIAL_PROMPT = (
    "입고 도크, 출고 도크, 회의실, 안전교육장, 검수장, 엘리베이터, 2층, 사무실, "
    "나가는 곳, 나가는 문, "
    # 말로만 알려주는 곳(`info_places`)도 넣는다. 빠져 있으면 whisper 가 후보로
    # 두지 않아 "화장실" 이 "허정실"·"상실이" 로 들리고, 그게 회의실·엘리베이터로
    # 잘못 매칭된다(2026-09-21 실측).
    "화장실, 계단, 흡연구역, 자판기, "
    "짐, 네, 아니요, 하차, 납품, 배송, 상차, 픽업, 출차, 검품, 품질, 검사"
)
